# PROJECT_CONTEXT.md — corehelperlab-site

> Живой документ. Обновляется в конце каждой сессии.
> При старте новой сессии — прочитать вместе с CLAUDE.md.

---

## Статус фич

| Фича | Статус | Заметки |
|------|--------|---------|
| Лендинг Fitence (fitence-workout/index.html) | ✅ В продакшене | Основной файл |
| Локализация EN / RU / UA | ✅ В продакшене | |
| Локализация FR / DE / ES / IT | ✅ В продакшене | |
| Выпадающий список языков с флагами | ✅ В продакшене | |
| Локализованные скриншоты (папки по языкам) | ✅ В продакшене | Фолбэк на `screens/en/`, папки для других языков пустые |

---

## Принятые решения

**Локализация**
- Переводы хранятся прямо в HTML как JS-объект — без внешних i18n библиотек. Осознанно: проект простой, лишняя зависимость не нужна.
- 61 ключ на язык.

**Скриншоты**
- Структура: `screens/<lang>/filename.jpg` с фолбэком на `screens/en/`
- Фолбэк реализован через JS `Image.onload/onerror` — без серверной логики
- Скриншоты для RU/UA/FR/DE/ES/IT пока пустые — добавляются по мере готовности, фолбэк работает автоматически
- Корневая папка `/screens/` устаревшая — не использовать

**Файлы**
- `fitence-workout-planner.html` — устаревший дубль, не использовать и не редактировать

**Git**
- Владелец может вносить правки напрямую в ветку — перед работой всегда делать `git pull`

---

## Открытые вопросы

- [ ] Нужны ли локализованные скриншоты для RU и UA (или только для новых языков)?
- [ ] Нужно ли обновить `fitence-workout/privacy.html` и `terms.html` под новые языки?
- [ ] Подключить Netlify для preview-деплоя при каждом PR?
